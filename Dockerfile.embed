FROM python:3.11-slim

WORKDIR /app

# Install uv for dependency management
RUN pip install --no-cache-dir uv

# Copy the embed script, the shared programme registry, and source files
COPY embed.py ./
COPY programs.py ./
COPY src ./src

# Create a requirements file for the dependencies
RUN echo "weaviate-client>=4.4.0" > requirements.txt && \
    echo "python-dotenv>=1.0.0" >> requirements.txt && \
    echo "packaging>=23.0" >> requirements.txt && \
    echo "psycopg[binary]>=3.2.0" >> requirements.txt && \
    echo "SQLAlchemy>=2.0.0" >> requirements.txt && \
    echo "pgvector>=0.3.0" >> requirements.txt

# Install dependencies
RUN uv pip install --system -r requirements.txt

# Copy Postgres FAQ bootstrap assets (seed + SQL)
COPY pg ./pg

# Default command: run the embedding script
CMD ["python", "embed.py"]
